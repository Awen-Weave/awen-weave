# Dolgellau Town Dataset — v0.1 Claim Schema

**Status:** draft for review. Supersedes v0 (`v0-schema.md`, kept for audit).
**Date:** 2026-05-10
**Driver:** the Tŷ Newyddion pilot (`pilot-ty-newyddion.md`) revealed six additive gaps in v0. v0.1 closes all six and resolves the two questions left open at v0 (predicate retirement, energy-study read pattern).
**Migration:** purely additive — every v0 claim remains valid under v0.1.

---

## 1. What's new from v0

Seven changes, in the order they appear in the schema below:

1. **`entity` gains a `visibility` column** — only meaningful for source entities (`public` | `restricted` | `private`). Read API filters source detail accordingly.
2. **`claim` gains three new value columns** — `value_entity_ref` (so claim values can point at other entities), `value_date_text` (human-readable original date expression like "c.1885"), and `qualifiers_json` (per-claim metadata like `name_type`, `floor_scope`, `date_precision`, `dialect`).
3. **Naming claims become multi-cardinality** with a required `name_type` qualifier (`current_local` / `listed_register` / `historic` / `vernacular`). The Tŷ Newyddion / Glyndwr Buildings / Newsagents case is now expressible.
4. **Three new entity types: `tenancy`, `event`, `research_question`** with their own predicate vocabularies. Tenancies handle occupancy timelines (Armitage → W H Smith → Wilkins → present); events handle refurbishments and designations; research questions promote known-unknowns to first-class records inside the Craidd.
5. **Entity-reference predicates** — `adjacent_to`, `contemporary_with`, `group_value_with`, `tenancy_of`, `affects_entity`, `relates_to_entity` — let the schema model the building-to-building relationships that listed-building "group value" depends on.
6. **`town` entity type** with a tightly-bounded starter predicate set (material tradition, street pattern, notable events, conservation authority, unitary authority, listed-building count, parish). Town-level facts are explicitly in scope where they bear on building histories; everything else stays out.
7. **Predicate deprecation mechanics** — predicates gain `deprecated_at` and `deprecation_reason`. Existing claims using a deprecated predicate stay valid; new claims using it are rejected. Closes the v0 open question.

Plus one operational decision:

8. **Energy-study read pattern: both API and nightly export.** A `GET /export/buildings.json` endpoint produces a public, signed snapshot every night, suitable for batch analysis (the energy study, future researchers). Live API stays the canonical query path. Closes the v0 open question.

## 2. Topology — unchanged from v0

```
Pi at Arloesi Dolgellau (canonical for data)
  /srv/town-dataset/
    craidd.duckdb    ← claims + sources  (initialised 2026-05-15)
    prawf.duckdb     ← append-only log   (initialised 2026-05-15)
    proposals/       ← pending claims
    evidence/        ← raw source files (incl. BRA snapshot bundles)
    exports/         ← nightly snapshots, signed
    src/             ← code (rsync'd from repo; not a git checkout yet)
    .venv/           ← duckdb, pandas
  Read API  :8080  (public, no auth)  + /export/* nightly snapshot      [not yet built]
  Write API :8443  (curator/contributor mTLS)                            [not yet built]
  MCP server :8081  (read-only)                                          [not yet built]

GitHub: arloesidolgellau/town-dataset (private; canonical for code/schema/design)
  design/            ← architecture, schema, designs, recon notes
  src/
    craidd/          ← schema layer + storage layer (built 2026-05-15)
    cli/             ← craidd-init (built); -fetch/-propose/-review/-export/-status to come
    bra/             ← Building Research Agent (v2 listings work in progress)
  client/            ← craidd_client.py
  config/agents/     ← per-agent BRA v2 site-shape manifests
  scripts/           ← curator wrappers (cron-friendly)
  seed/              ← bootstrap data + filter scripts
  tests/             ← (placeholder)
```

Storage and authority unchanged: Pi is canonical for data; repo is canonical for code, schema, and design. If Pi and repo disagree about data, the Pi wins. If they disagree about schema or design, the repo wins. The energy study stays where it is and becomes a read+propose client.

Disjoint-tree discipline: `src/bra/` is the BRA work; `src/craidd/` and `src/cli/` are the foundation. BRA imports `from craidd.schema import ...` but never modifies the foundation. Future foundation work proceeds in `src/craidd/` / `src/cli/` without touching `src/bra/`. See `CLAUDE.md` for the rule statement.

## 3. Schema

### 3.1 `entity`

```sql
CREATE TABLE entity (
  entity_id     VARCHAR PRIMARY KEY,
  entity_type   VARCHAR NOT NULL,        -- see §3.4 for the controlled list
  uprn          BIGINT,
  toid          VARCHAR,
  visibility    VARCHAR,                 -- 'public' | 'restricted' | 'private'
                                         -- only meaningful when entity_type='source';
                                         -- null otherwise
  created_at    TIMESTAMP NOT NULL DEFAULT NOW(),
  notes         TEXT
);
```

Visibility semantics on the read API:

- `public` (default) — cite normally with title, organisation, URL, accessed_at.
- `restricted` — cite title and organisation only; URL and detailed evidence withheld; the read API returns "restricted source" markers in the place of detail.
- `private` — cite as "internal source" with the entity_id as the only externally-visible reference. Useful and honest: the public sees that *a* source exists and is recorded, just not what it is.

Internal Huw documents (the Tŷ Newyddion Operations Memo, the email to Coleg Cambria) become `visibility: private` source entities.

### 3.2 `claim`

```sql
CREATE TABLE claim (
  claim_id          VARCHAR PRIMARY KEY,
  subject_id        VARCHAR NOT NULL REFERENCES entity(entity_id),
  predicate         VARCHAR NOT NULL REFERENCES predicate(name),
  value_text        VARCHAR,
  value_int         BIGINT,
  value_real        DOUBLE,
  value_date        DATE,
  value_date_text   VARCHAR,                  -- NEW: "c.1885", "from 2016", "late C19"
  value_geom        GEOMETRY,
  value_cy          VARCHAR,
  value_en          VARCHAR,
  value_entity_ref  VARCHAR REFERENCES entity(entity_id),  -- NEW: for relationships
  qualifiers_json   VARCHAR,                  -- NEW: claim-level metadata, see below
  source_id         VARCHAR NOT NULL REFERENCES entity(entity_id),
  recorded_by       VARCHAR NOT NULL,
  recorded_at       TIMESTAMP NOT NULL DEFAULT NOW(),
  confidence        VARCHAR NOT NULL CHECK (confidence IN ('high','medium','low')),
  evidence_uri      VARCHAR,
  superseded_by     VARCHAR REFERENCES claim(claim_id),
  status            VARCHAR NOT NULL DEFAULT 'active'
                    CHECK (status IN ('active','superseded','disputed','withdrawn'))
);

CREATE INDEX claim_subject_predicate_idx ON claim(subject_id, predicate);
CREATE INDEX claim_status_idx ON claim(status);
CREATE INDEX claim_value_entity_ref_idx ON claim(value_entity_ref);
```

**Qualifiers** are claim-level metadata that aren't themselves claims. They describe *this* claim — its dialect, its applicable floor, its date precision, its name type — without warranting separate provenance. Controlled vocabulary in v0.1:

| Key | Domain | Values |
|---|---|---|
| `dialect` | bilingual claims with cy populated | `cy-GB-north` (default for Dolgellau) / `cy-GB-south` / `cy-GB-mid` / `cy-GB` (unspecified) / other ISO codes |
| `name_type` | claims on `name_cy` / `name_en` | `current_local` / `listed_register` / `historic` / `vernacular` |
| `floor_scope` | tenancy and event claims | `whole` / `ground` / `first` / `upper` / `attic` / `basement` / free-text where needed |
| `date_precision` | claims with `value_date` | `exact` / `year` / `decade` / `century` / `range` |

Why qualifiers and not predicates: a dialect is not a fact about the *building*. It's a fact about *this claim about the building*. Promoting them to predicates would make every claim bilingual into two claims, doubling the row count without adding provenance value. Qualifiers travel with the claim they describe.

### 3.3 `predicate` — with deprecation

```sql
CREATE TABLE predicate (
  name                 VARCHAR PRIMARY KEY,
  value_type           VARCHAR NOT NULL CHECK (value_type IN
                       ('text','int','real','date','geom','bilingual','entity_ref')),
  cardinality          VARCHAR NOT NULL CHECK (cardinality IN ('single','multi')),
  applies_to_types     VARCHAR NOT NULL,          -- JSON array, e.g. '["building","tenancy"]'
  description_cy       VARCHAR NOT NULL,
  description_en       VARCHAR NOT NULL,
  constraint_json      VARCHAR,
  required_qualifiers  VARCHAR,                   -- JSON array, e.g. '["name_type"]'
  added_at             TIMESTAMP NOT NULL DEFAULT NOW(),
  added_by             VARCHAR NOT NULL,
  deprecated_at        TIMESTAMP,                 -- NEW: null while active
  deprecated_by        VARCHAR,                   -- NEW
  deprecation_reason   VARCHAR,                   -- NEW
  superseded_by_predicate VARCHAR REFERENCES predicate(name)
                                                  -- NEW: optional pointer to replacement
);
```

Adding a predicate is a deliberate act, recorded in Prawf with action `predicate_added`. Deprecation is also a Prawf-recorded act (`predicate_deprecated`). Existing claims under a deprecated predicate remain queryable; new claims using it are rejected at the write API. Deprecated predicates appear in `GET /predicates?include=deprecated` for tools that need to read history.

`required_qualifiers` lets the schema enforce, for example, that every `name_cy` claim carry a `name_type` qualifier in `qualifiers_json`. The write API validates this.

`applies_to_types` lets the schema enforce, for example, that `tenant_name` is only valid on `tenancy` entities, `material_tradition` only on `town` entities, etc. Wrong-type claims are rejected at the write API.

### 3.4 Entity types

v0.1 controlled vocabulary for `entity_type`:

| Type | Purpose | Notes |
|---|---|---|
| `building` | A physical building or substantial structure. | UPRN/TOID anchors where available. |
| `street` | A named street or street segment. | Dolgellau-relative. |
| `area` | A bounded area — conservation area, ward, parish. | Carries a polygon. |
| `town` | The town as a whole subject. | Single instance for v1: TDS-DOL-T-00001. |
| `tenancy` | An occupancy of a building (or floor) by a tenant over a period. | Has `tenancy_of`, `tenant_name`, `period_start`, `period_end`, `tenancy_type`, optionally `floor_scope`. |
| `event` | A dated thing that happened. | Designations, refurbishments, changes of use, sales, fires, floods, demolitions. |
| `research_question` | A known-unknown the dataset is tracking. | Has `question_text`, `relates_to_entity`, `status`, optionally `answered_by_claim`. |
| `source` | A citation. | Carries `visibility`. |
| `person` | A named individual relevant to a building's history. | Used sparingly in v1 (architect, notable owner, founder). |

`person` is included because the Armitage / W H Smith founder / W D Wilkins line in the Tŷ Newyddion record will eventually want named individuals. In v1 we mention `person` exists but only create them when an actual research question resolves. Don't pre-populate.

### 3.5 Starter predicate set — full v0.1

**Open items surfaced during the foundation build (2026-05-15)** — tracked here rather than scattered through tickets:

- **Welsh predicate descriptions pending a proper pass.** `predicate.description_cy` is `NOT NULL` in the DDL (§11), but the tables below supply English meanings only. The foundation seeds all 58 predicates with the placeholder string `"(Welsh description pending)"` (`src/craidd/schema/predicates.py`) rather than machine-translating — that would violate the project's bilingual discipline (§4: "Welsh stored when known, null when unknown — honest coverage"). A proper Welsh pass is a curator task for Huw with his tutor; treat the placeholders as known-honest gaps until that happens.
- **`street` and `area` entity types have no predicates in this seed set.** Both are declared in §3.4 as valid entity types but the §3.5 tables enumerate no predicates for them. v0.2 should either seed predicates for both or remove them from the entity-type list.
- **`building_type` is marked "controlled enum" but the enum is undefined.** The starter table below labels `building_type` as a controlled enum without listing valid values. v0.2 should pick the value set (likely a small list aligned with EPC's property types and Cadw's building classifications).
- **`listed_building_count` references an `accessed_at` qualifier not in §3.2's vocabulary.** Either add `accessed_at` to the qualifier set (it's already implicit in source-side claims) or rework the predicate to carry the date as part of the claim's `value_date`.

Building predicates (mostly v0, with adjustments):

| name | type | cardinality | required qualifiers | meaning |
|---|---|---|---|---|
| `address` | bilingual | single | — | Postal address |
| `geometry` | geom | single | — | Building footprint or point |
| `uprn` | int | single | — | OS UPRN |
| `building_type` | text | single | — | Controlled enum |
| `floor_area_m2` | real | single | — | Total internal floor area |
| `build_year` | int | single | — | Year built (use only when date is exact) |
| `build_period` | text | single | `date_precision` | Imprecise period — "c.1885", "late C18" |
| `original_use` | bilingual | multi | — | Historic primary use(s) |
| `current_use` | bilingual | single | — | Today's primary use |
| `listed_grade` | text | single | — | enum: I / II* / II |
| `listed_id` | text | multi | — | Cadw or BLB references; multi-cardinality so multiple register IDs can co-exist |
| `conservation_area` | text | multi | — | CAs the building sits within |
| `name_cy` | text | **multi** | **`name_type`** | Welsh name(s) |
| `name_en` | text | **multi** | **`name_type`** | English name(s) |
| `historical_note` | bilingual | multi | — | Free-text historical claim |
| `architectural_description` | bilingual | multi | — | Structured architectural detail (NEW, replaces overuse of `historical_note`) |
| `material_primary` | text | single | — | e.g. "snecked rubble dolerite" |
| `roof_type` | text | single | — | e.g. "hipped slate" |
| `storeys` | int | single | — | Number of full storeys |
| `adjacent_to` | entity_ref | multi | — | Other buildings physically adjacent |
| `contemporary_with` | entity_ref | multi | — | Buildings of same construction period |
| `group_value_with` | entity_ref | multi | — | Buildings whose listing reason is shared/related |

Tenancy predicates (apply only to `entity_type='tenancy'`):

| name | type | cardinality | required qualifiers | meaning |
|---|---|---|---|---|
| `tenancy_of` | entity_ref | single | — | The building (or area) the tenancy occupies |
| `tenant_name` | text | single | — | Common name of the tenant |
| `tenant_organisation` | text | single | — | Formal organisation name where applicable |
| `tenancy_type` | text | single | — | enum: commercial_retail / commercial_wholesale / residential / office / hospitality / community / mixed / vacant / other |
| `tenant_business_type` | bilingual | single | — | e.g. "newsagents and bookshop", "music salesroom" |
| `period_start` | date | single | `date_precision` | Earliest plausible start |
| `period_end` | date | single | `date_precision` | Earliest plausible end; null = current |

Event predicates (apply only to `entity_type='event'`):

| name | type | cardinality | required qualifiers | meaning |
|---|---|---|---|---|
| `event_type` | text | single | — | enum: refurbishment / designation / change_of_use / sale / construction / demolition / fire / flood / other |
| `event_start` | date | single | `date_precision` | Start date |
| `event_end` | date | single | `date_precision` | End date; null = ongoing |
| `affects_entity` | entity_ref | multi | — | Entities this event acts upon |
| `funder` | entity_ref | multi | — | Where the funder is itself an entity |
| `funder_text` | text | multi | — | Where the funder is recorded as a string only |
| `scope_description` | bilingual | single | — | What the event did |
| `consent_reference` | text | multi | — | LBC, planning, designation reference |

Research-question predicates (apply only to `entity_type='research_question'`):

| name | type | cardinality | required qualifiers | meaning |
|---|---|---|---|---|
| `question_text` | bilingual | single | — | The question itself |
| `relates_to_entity` | entity_ref | multi | — | Entities the question is about |
| `suggested_sources` | text | multi | — | Where to look (free text) |
| `priority` | text | single | — | enum: low / medium / high |
| `status` | text | single | — | enum: open / in_progress / answered / abandoned |
| `answered_by_claim` | text | single | — | claim_id pointing at the resolving claim |

Source predicates (`entity_type='source'`):

| name | type | cardinality | meaning |
|---|---|---|---|
| `title_cy` | text | single | Welsh title where applicable |
| `title_en` | text | single | English title |
| `citation` | text | single | Full citation string |
| `url` | text | single | Canonical URL |
| `organisation` | text | single | Authoring/holding organisation |
| `licence` | text | single | OGL, CC-BY-SA, internal, etc. |
| `accessed_at` | date | single | Most recent retrieval date |
| `file_hash` | text | single | SHA-256 of the evidence file, when applicable |

Town predicates (`entity_type='town'`, tightly bounded for v1):

| name | type | cardinality | meaning |
|---|---|---|---|
| `material_tradition` | bilingual | multi | e.g. "snecked dark grey dolerite with freestone dressings, Welsh slate roofs" |
| `street_pattern` | bilingual | single | Narrative description |
| `notable_event` | bilingual | multi | Railway opening 1865/1869, gold rush, etc. |
| `conservation_authority` | text | single | LPA for conservation consent |
| `unitary_authority` | text | single | Council for non-planning matters |
| `listed_building_count` | int | single | With `accessed_at` qualifier (date the count was made) |
| `parish` | text | single | Ecclesiastical parish where relevant |

This is the complete v0.1 vocabulary as defined in §3.5: **58 predicates** spanning building, tenancy, event, research_question, source, and town. Adding more is a deliberate act, logged in Prawf.

(The earlier prose-summary count of "52" was a tally error in this document — the §3.5 tables above are authoritative, and the foundation's `craidd.schema.PREDICATE_REGISTRY` reflects 58 entries. Corrected 2026-05-15 during the foundation handover reconciliation.)

**Update 2026-05-16 (§10 item 7 landed):** the live `PREDICATE_REGISTRY` is now **60 entries** — the 58 above plus `verified_building_toid` and `location_verification_status` from the Lleolydd work (§10 item 7.1). The two additions live in `_BUILDING` in `src/craidd/schema/predicates.py` rather than in the §3.5 table above; this section continues to describe the v0.1 baseline vocabulary, and §10 item 7 is the canonical home for the additions' specification.

## 4. Bilingual handling — unchanged from v0

Two patterns: paired fields on a single claim (default) versus separate claims with parallel predicates. Welsh stored when known, null when unknown — honest coverage. Dialect default `cy-GB-north`; deviations tagged on the claim's qualifiers.

## 5. Read interface — v0 plus export

All v0 endpoints unchanged. Two additions:

```
GET /export/buildings.json     # nightly signed snapshot
GET /export/<entity_type>.json # per-type snapshots
```

The nightly snapshot is generated at 03:00 local time, signed with the curator key, and held for 30 days under `/srv/town-dataset/exports/`. The energy study (and any other batch consumer) pulls this file rather than hammering the live API. The signed digest is published in Prawf so anyone can verify a copy they hold matches what the Pi served.

```
GET /predicates?include=deprecated   # NEW — include retired predicates
GET /research-questions?status=open  # NEW — discover open research
GET /buildings/{id}/relationships    # NEW — adjacent_to, group_value_with, etc.
```

## 6. Write interface — unchanged from v0

Two-tier curator/contributor model with mTLS authentication. Proposal queue for all non-curator writes. Self-acceptance forbidden at the API layer. The proposal review CLI (`craidd-review`) is updated to handle the new entity types — accepting a tenancy proposal creates the tenancy entity and its claims atomically.

## 7. Worked example — Tŷ Newyddion under v0.1

This re-does §3 of the pilot record using v0.1 expressiveness. The previously-stretched parts now fit cleanly.

### 7.1 Names — three claims, one per name_type

```yaml
- subject: TDS-DOL-B-00001
  predicate: name_cy
  value_text: "Tŷ Newyddion"
  qualifiers: { name_type: current_local, dialect: cy-GB-north }
  source: TDS-DOL-SRC-INTERNAL-HUW-2026
  confidence: high

- subject: TDS-DOL-B-00001
  predicate: name_en
  value_text: "Glyndwr Buildings"
  qualifiers: { name_type: current_local }
  source: TDS-DOL-SRC-INTERNAL-HUW-2026
  confidence: high
  note: "Local English-language name in active use."

- subject: TDS-DOL-B-00001
  predicate: name_en
  value_text: "Newsagents"
  qualifiers: { name_type: listed_register }
  source: TDS-DOL-SRC-CADW-4938
  confidence: high
  note: "Cadw register working name, derived from the C20 W H Smith
         and W D Wilkins occupancy. Working name only, not vernacular."

- subject: TDS-DOL-B-00001
  predicate: name_en
  value_text: "Glyndwr Building"
  qualifiers: { name_type: vernacular }
  source: TDS-DOL-SRC-INTERNAL-HUW-2026
  confidence: medium
  note: "Singular variant occasionally seen — preserved as a real
         micro-contradiction with the plural form above."
```

### 7.2 Tenancies — four entities

Armitage:

```yaml
- entity_id: TDS-DOL-Y-00001
  entity_type: tenancy

- subject: TDS-DOL-Y-00001
  predicate: tenancy_of
  value_entity_ref: TDS-DOL-B-00001
  qualifiers: { floor_scope: ground }
  source: TDS-DOL-SRC-INTERNAL-HUW-2026
  confidence: high

- subject: TDS-DOL-Y-00001
  predicate: tenant_name
  value_text: "Armitage"
  source: TDS-DOL-SRC-BLB-300004938
  confidence: medium
  note: "Identity unknown — see research_question TDS-DOL-Q-00001."

- subject: TDS-DOL-Y-00001
  predicate: tenancy_type
  value_text: "commercial_wholesale"
  source: TDS-DOL-SRC-BLB-300004938
  confidence: medium

- subject: TDS-DOL-Y-00001
  predicate: tenant_business_type
  value_en: "warehouse and music salesroom"
  source: TDS-DOL-SRC-BLB-300004938
  confidence: medium

- subject: TDS-DOL-Y-00001
  predicate: period_start
  value_date: 1885-01-01
  value_date_text: "c.1885"
  qualifiers: { date_precision: year }
  source: TDS-DOL-SRC-CADW-4938
  confidence: low
  note: "Inferred from building construction date."

- subject: TDS-DOL-Y-00001
  predicate: period_end
  value_date: null
  source: null
  confidence: low
  note: "Unknown — research_question TDS-DOL-Q-00001."
```

W H Smith, W D Wilkins, Arloesi Dolgellau (current ground floor), and Tŷ Newyddion hospitality (current upper floors) become parallel tenancy entities, each with `floor_scope` populated where the occupancy is part-building.

### 7.3 Events — designation and refurbishment

The 1990 designation:

```yaml
- entity_id: TDS-DOL-E-00001
  entity_type: event

- subject: TDS-DOL-E-00001
  predicate: event_type
  value_text: "designation"
  source: TDS-DOL-SRC-CADW-4938

- subject: TDS-DOL-E-00001
  predicate: event_start
  value_date: 1990-06-19
  qualifiers: { date_precision: exact }
  source: TDS-DOL-SRC-CADW-4938
  confidence: high

- subject: TDS-DOL-E-00001
  predicate: affects_entity
  value_entity_ref: TDS-DOL-B-00001
  source: TDS-DOL-SRC-CADW-4938

- subject: TDS-DOL-E-00001
  predicate: scope_description
  value_en: "Statutory listing at Grade II by Cadw, reason 'group value'."
  source: TDS-DOL-SRC-CADW-4938

- subject: TDS-DOL-E-00001
  predicate: consent_reference
  value_text: "Cadw 4938"
  source: TDS-DOL-SRC-CADW-4938
```

The Townscape Heritage Project refurbishment (analogous shape — `event_type: refurbishment`, `event_start: 2016`, `funder: TDS-DOL-SRC-NLHF` plus the SNPA, scope description capturing structural repair + heat pumps + reconfiguration to eight bedrooms and Boardroom).

### 7.4 Relationships — group value with the Glyndwr Milk Bar

```yaml
- subject: TDS-DOL-B-00001
  predicate: adjacent_to
  value_entity_ref: TDS-DOL-B-00002
  source: TDS-DOL-SRC-CADW-4938
  confidence: high

- subject: TDS-DOL-B-00001
  predicate: contemporary_with
  value_entity_ref: TDS-DOL-B-00002
  source: TDS-DOL-SRC-CADW-4938
  confidence: high

- subject: TDS-DOL-B-00001
  predicate: group_value_with
  value_entity_ref: TDS-DOL-B-00002
  source: TDS-DOL-SRC-CADW-4938
  confidence: high
  note: "Both Cadw 4938 and 4939 carry 'group value' as the listing reason —
         the relationship is the listing reason, not just an adjacency."
```

### 7.5 Research questions — seven, one entity each

The seven items in §9 of the source document become seven `research_question` entities. One example:

```yaml
- entity_id: TDS-DOL-Q-00001
  entity_type: research_question

- subject: TDS-DOL-Q-00001
  predicate: question_text
  value_en: "Who or what was 'Armitage'? Identify the family, firm or
             regional branch and the dates of their occupation of
             Tŷ Newyddion."
  source: TDS-DOL-SRC-INTERNAL-HUW-2026

- subject: TDS-DOL-Q-00001
  predicate: relates_to_entity
  value_entity_ref: TDS-DOL-B-00001
  source: TDS-DOL-SRC-INTERNAL-HUW-2026

- subject: TDS-DOL-Q-00001
  predicate: relates_to_entity
  value_entity_ref: TDS-DOL-Y-00001
  source: TDS-DOL-SRC-INTERNAL-HUW-2026

- subject: TDS-DOL-Q-00001
  predicate: suggested_sources
  value_text: "Slater's, Sutton's, Worrall's late-Victorian trade directories;
               Caernarfon Archive (Gwynedd Archives, North Wales County
               Record Office)."
  source: TDS-DOL-SRC-INTERNAL-HUW-2026

- subject: TDS-DOL-Q-00001
  predicate: status
  value_text: "open"
  source: hwt
```

When the question is answered, a normal claim is added (e.g. on `tenant_name` or `period_end` of TDS-DOL-Y-00001) and the question's `status` becomes `answered` with `answered_by_claim` populated. The unknown is now known *and* the path to its resolution is recorded.

### 7.6 Town-level claims

```yaml
- subject: TDS-DOL-T-00001
  predicate: material_tradition
  value_en: "Dark grey dolerite worked into snecked rubble walling, dressed
             with freestone, roofed in Welsh slate."
  source: TDS-DOL-SRC-CADW-UUC
  confidence: high

- subject: TDS-DOL-T-00001
  predicate: notable_event
  value_en: "Cambrian Railways station opened 3 July 1865 (temporary, west
             of Bridge Street bridge); permanent Dolgelly station opened
             1 August 1869."
  source: TDS-DOL-SRC-WIKIPEDIA-DOLGELLAU
  confidence: medium
  note: "Wikipedia secondary source — corroboration from primary railway
         records would upgrade to high."

- subject: TDS-DOL-T-00001
  predicate: conservation_authority
  value_text: "Snowdonia (Eryri) National Park Authority"
  source: TDS-DOL-SRC-CADW-UUC
  confidence: high

- subject: TDS-DOL-T-00001
  predicate: unitary_authority
  value_text: "Gwynedd Council"
  source: TDS-DOL-SRC-CADW-UUC
  confidence: high
```

## 8. Decisions taken in v0.1

**Predicate retirement: soft-deprecation.** A predicate gets `deprecated_at`, `deprecated_by`, `deprecation_reason`, and optionally `superseded_by_predicate`. Existing claims using it remain valid and queryable. New claims using a deprecated predicate are rejected at the write API. Deprecated predicates appear in `GET /predicates?include=deprecated`. Migrating claims to a successor predicate is a deliberate curator action, logged in Prawf as a supersession chain.

**Energy-study read pattern: API plus nightly export.** Live API for ad-hoc and authenticated access; signed nightly JSON snapshot at `/export/*.json` for batch consumers. The energy study pulls the snapshot for its own pipeline runs and uses the live API for spot-checks. Export digests are recorded in Prawf so any consumer can verify integrity.

**Dates with imprecision: hybrid.** `value_date` holds a sortable canonical date (the earliest plausible interpretation by convention). `value_date_text` holds the original imprecise form ("c.1885", "late C19", "from 2016"). `qualifiers.date_precision` records the precision (`exact` / `year` / `decade` / `century` / `range`). The API surfaces `value_date_text` to humans and uses `value_date` for filtering and sorting. EDTF (Library of Congress Extended Date/Time Format) is the eventual destination but unnecessary for v1.

**Person entities: declared but not pre-populated.** `entity_type='person'` is in the controlled vocabulary so research-question answers (Armitage, the c.1885 architect, the Wilkins family) can land cleanly when discovered. We do not pre-create person entities or populate genealogical fields. v1 is buildings + histories; people enter only as the answers to specific research questions.

## 9. Migration from v0

Purely additive. Concrete steps when v0 is implemented and v0.1 follows:

1. ALTER TABLE entity ADD COLUMN visibility VARCHAR;
2. ALTER TABLE claim ADD COLUMN value_entity_ref VARCHAR REFERENCES entity(entity_id);
3. ALTER TABLE claim ADD COLUMN value_date_text VARCHAR;
4. ALTER TABLE claim ADD COLUMN qualifiers_json VARCHAR;
5. ALTER TABLE predicate ADD COLUMN applies_to_types VARCHAR;
6. ALTER TABLE predicate ADD COLUMN required_qualifiers VARCHAR;
7. ALTER TABLE predicate ADD COLUMN deprecated_at TIMESTAMP;
8. ALTER TABLE predicate ADD COLUMN deprecated_by VARCHAR;
9. ALTER TABLE predicate ADD COLUMN deprecation_reason VARCHAR;
10. ALTER TABLE predicate ADD COLUMN superseded_by_predicate VARCHAR REFERENCES predicate(name);
11. UPDATE predicate SET cardinality = 'multi' WHERE name IN ('name_cy','name_en');
    -- existing single-cardinality claims under these predicates remain valid;
    -- add the name_type qualifier retroactively where evident, or accept null.
12. INSERT new predicates and entity types as listed in §3.

Every v0 claim continues to validate. Existing v0 records get the new qualifier slots populated lazily by curators as they revisit each claim — no forced backfill.

## 10. Still open — for v0.2

Seven genuinely deferred questions. The third was added 2026-05-10 after a curator review of the Tŷ Newyddion pilot; the fourth was added 2026-05-12 after the HM Land Registry OCOD dataset surfaced ownership data the schema can't currently model; the fifth and sixth were added 2026-05-12 during the BRA v2 build (richness pilot + production-pilot sign-off); the seventh was added 2026-05-16 during the Lleolydd design pass, covering UPRN-verification predicates plus the qualifiers needed for co-signed field-session acceptance.

1. **Floor- and unit-level sub-entities.** v0.1 handles floors via the `floor_scope` qualifier on tenancy and event claims. That works for v1 buildings + histories. When residential conversions, listed-building consents at unit level, or per-flat tenancies become the focus, sub-entities (`entity_type='unit'` with a `unit_of` relationship) will be required.
2. **EDTF dates.** The hybrid approach in §8 is good enough for v1. When research-question answers start producing date ranges of forms the hybrid can't carry cleanly ("between 1898 and 1903 inclusive", "before 1914"), migrate `value_date_text` to EDTF and add an EDTF parser in the read API.
3. **`listing` as a distinct entity type from `building`.** v0.1 conflates the two: a building is what's physically there, a listing is what Cadw chose to record, and v0.1 treats them as the same kind of subject — claims about a building include `listed_id` claims that point at the register entry. This works *within* the rule "model the physical building, attach listings to it as multi-cardinality claims," but it breaks down on real cases:
    - **Tŷ Newyddion / "Glyndwr Milk Bar"** (Cadw 4938 + 4939) — one physical building, two listings. Modelled in v0 as two separate buildings, then corrected on 2026-05-10 to one building with two `listed_id` claims. The correction works but loses structure: there's no clean way to attach claims that belong specifically to one register entry rather than the other.
    - **Tanrallt / Adre on Mount Pleasant** — one original building divided into two ownerships, *only one Cadw listing*, and that listing describes features that are now in the *other* ownership. v0.1 has no way to express "this listing describes features that are not all in the entity it's attached to."
    - General principle: physical buildings and Cadw register entries are not one-to-one. One building can carry zero, one, or several listings. One listing can cover one, several, or partial buildings. Modelling listings as if they were buildings is structurally wrong.
    
    **v0.2 proposal:** introduce `entity_type='listing'`. A listing has its own claims (`listing_authority`, `listing_grade`, `listing_reason`, `listing_date`, `register_id`, `feature_described`, `description_text`, `archive_url`). A listing relates to one or more buildings via a `lists` predicate (entity_ref, multi). A listing's described features (sash details, materials, dormers, etc.) live on the *listing*, not on the building — this resolves the Tanrallt/Adre case where the listing describes features outside its associated entity. Buildings continue to carry physical claims (geometry, material, roof_type, storeys); listings carry register claims (grade, register_id, authority). The two are joined explicitly. Migration from v0.1 is mechanical: each `listed_id` claim becomes a new `listing` entity, and the building gains a `lists` relationship in reverse.

4. **Proprietorship and sales history as v0.2 expansion.** Added 2026-05-12 after the HM Land Registry OCOD national dataset surfaced two foreign-owned properties in the Dolgellau area: *Woodlands at Cwm Mynach, Bontddu* (Jersey trust) and *The Pharmacy and Bodfan, Queens and Eldon Square, Dolgellau* (Florida LLC, £190,000 registered 16 June 2025). v0.1 has no clean way to record proprietorship; v0.2 should introduce:
    - **`entity_type='proprietor'`** — a registered owner with claims for `proprietor_name`, `proprietorship_category` (limited company / corporate body / trust / etc.), `country_incorporated`, `company_registration_number`, `proprietor_service_address`. Where multiple Dolgellau properties share an overseas owner, the proprietor resolves to one entity, not three text strings.
    - **`entity_type='sale_record'`** — a transactional event with claims for `sale_date`, `sale_price` (int, GBP), `tenure` (freehold / leasehold), `title_number` (HMLR reference), `buyer_class`, `seller_class`. Each building accumulates a chronological list of sale_record entities — answers "how long has someone owned this?"
    - **New predicates on `building`**: `proprietor` (entity_ref, multi — a building can have joint proprietors, e.g. the Bontddu case has two Jersey trust companies), `hm_land_registry_title` (text, multi), `most_recent_sale` (entity_ref to a sale_record, single — for fast canonical-view lookup), `date_proprietor_added` (date, single).
    - **Source entities**: `TDS-DOL-SRC-HMLR-OCOD` (monthly published OCOD data, OGL), `TDS-DOL-SRC-HMLR-PRICE-PAID` (Price Paid Data, OGL).

    **Implementation note for v0.2:** seed scripts at `seed/filter-ocod-dolgellau.py` and `seed/filter-pricepaid-dolgellau.py` (both committed on `main` 2026-05-15) already produce filtered Dolgellau-area subsets at `seed/output/dolgellau-ocod-YYYY-MM.csv` and `seed/output/dolgellau-pricepaid-YYYY.csv`. These filtered subsets feed a proposal-batch generator that produces curator-reviewable claims via the Craidd client. **Ethics note**: OCOD and Price Paid Data are explicitly public for transparency reasons; the Town Dataset's aggregation discipline (source-tagged claims, public Prawf, curator-mediated) handles them without inferring private facts about individuals.

5. **BRA v2 — estate-agent listings: predicate and qualifier additions.** Surfaced 2026-05-12 by the BRA v2 design and richness pilot (`design/bra-v2-estate-agents.md`, `design/bra-v2-estate-agents-pilot.md`). The pilot scored two live Walter Lloyd Jones listings against the field catalogue and confirmed three new predicates plus one new `floor_area_basis` value are needed before BRA v2's stage-2 claim-extraction can run:

    - **`planning_restriction`** — enum + verbatim text. Enum values: `holiday_let_only` / `agricultural_tie` / `affordable_housing_only` / `commercial_use_class` / `none`. Paired with a free-text qualifier carrying the verbatim restriction wording from the listing. Materially affects use class, council tax, and saleability — Beudy Talywaen in the pilot is holiday-let-only with primary residence prohibited; not a peripheral detail.
    - **`epc_exempt`** — boolean + reason enum. Reason values: `listed_building` / `holiday_let` / `place_of_worship` / `other`. Listed buildings show EPC as exempt and the listing surfaces this as a positive feature; recording exemption explicitly is better than recording absence (the alternative — an empty `epc_band` — is indistinguishable from "not yet measured").
    - **`listing_events`** — array predicate carried on the estate-agent listing entity (see naming note below). Each entry: `event_type` (`added` / `reduced` / `sold-stc` / `sold` / `withdrawn` / `let-agreed` / `let`) and `event_date`. Aggregators surface this trivially ("Added on …", "Reduced on …", "Sold STC on …"); capturing it gives a free lifecycle history.
    - **New value for the `floor_area_basis` qualifier domain: `computed-from-room-dimensions`.** Joins the existing basis vocabulary (`gia` / `nia` / `from-epc` / `agent-stated` / `measured-survey`). Pilot finding (2026-05-12) is that headline GIA on Rightmove-shaped listings is usually "Ask agent", but room-by-room metric dimensions are stated for every room — summing them gives a reliable internal floor area, often more trustworthy than agent-stated GIA. Cross-domain valuable for the energy study's tier-4 default-floor-area placeholder rows.

    **Naming-collision note with item 3 above.** Item 3 uses `entity_type='listing'` for Cadw register entries. BRA v2's `listing_events` predicate, and the wider notion of an estate-agent's marketed offering with a lifecycle, live on a structurally different concept that also colloquially calls itself a "listing." v0.2 must disambiguate at schema time — recommended candidate names: `register_entry` (or `cadw_listing`) for the heritage-register side, `marketing_listing` (or `agent_listing`) for the estate-agent side. Whichever resolution lands, the predicates added in this item 5 attach to the estate-agent side, not the heritage-register side. The BRA v2 design doc currently uses the bare `listing` term for the estate-agent concept; that document will be updated once v0.2 picks the disambiguation.

    **Note on naming overlap with item 4.** Item 4 introduces `entity_type='sale_record'` (an HMLR-shaped transactional event). The BRA v2 design doc §6 referenced `sale_record` as already-in-the-v0.2-backlog, anticipating this convergence — the two items together establish the full sale + listing model: HMLR provides the canonical sale fact; BRA v2 provides the listing-side narrative and (where the dates align) the agent-side context for that sale.

6. **`agent` as a v0.2 entity type.** Decided 2026-05-12 during BRA v2 open-question sign-off (`design/bra-v2-estate-agents.md` §8(d)). The estate agent (Walter Lloyd Jones & Co., Savills, RG Jones, etc.) becomes a first-class Craidd entity in v0.2, not a config-only allowlist.

    **Why.** Listings carry their agent's identity; agents change ownership (mergers, brand changes, founder transitions); agent identity is provenance for `sale_record` and `marketing_listing` claims; the discipline of treating sources as entities (already established in v0.1) extends naturally. A config-only allowlist would lose history and contradict the v0.1 source-as-entity pattern.

    **Starter predicates** (additive over v0.1, mirroring the `town` entity's tightly-bounded starter set in v0.1 §3.5):

    - `agent_name` (bilingual, single) — current trading name.
    - `agent_legal_name` (text, single) — Companies House registered name where different.
    - `branch` (text, multi) — branch labels (e.g. "Dolgellau"); paired with `branch_address` and per-branch postcode qualifier.
    - `branch_address` (text, multi, paired with `branch`) — the postal address of each branch.
    - `founded_year` (int, single) — Walter Lloyd Jones & Co. was founded 1905; first-rate Awen-grade provenance.
    - `parent_company` (entity_ref, single) — points to another `agent` entity where applicable (acquisitions, group structures).
    - `website` (text, single).
    - `agency_type` (enum, multi) — `sales` / `lettings` / `commercial` / `auction` / `land_agency`.

    **Migration path from v0/v0.1 config allowlist.** The interim curator-approved CSV at `seed/agents/dolgellau-agents.csv` (produced by BRA v2 stage 1) becomes the seed for `agent` entity entries when v0.2 lands. Each CSV row → one `agent` entity + a starter set of claims. No retro-rewriting of any existing BRA v2 evidence; the listings keep their per-listing agent_name claim and the v0.2 migration adds the `marketing_listing → agent` reference predicate as a separate enrichment claim.

7. **Lleolydd-driven additions.** Surfaced by the Lleolydd design (`design/lleolydd.md`, 2026-05-16). All purely additive over v0.1 — no breaking changes. Defers nothing structural; existing claims continue to validate unchanged.

    **7.1 New predicates.**

    | Predicate | Subject | Value type | Cardinality | Bilingual | Notes |
    |---|---|---|---|---|---|
    | `verified_building_toid` | building | `toid` (string, OS MasterMap TopographicArea identifier, format `osgb<digits>`) | 1 (latest wins; superseded entries retained in history) | n/a | The TOID a curator has explicitly confirmed represents this building's footprint. Distinct from any auto-snapped TOID, which lives only as a derivation. |
    | `location_verification_status` | building, UPRN | `enum` (`verified`, `auto-snapped`, `unsnapped`, `contested`, `non-postal`) | 1 (derived; materialised) | n/a | The status band Lleolydd colour-codes by. Derived from the live claims plus the broadcast layer's pending placements; refreshed on proposal acceptance, cache rebuild, and broadcast tick. |

    **7.2 Extended `geometry_basis` qualifier vocabulary.** The existing `geometry` predicate already accepts a `geometry_basis` qualifier. Extend its enum with four Lleolydd-relevant values:

    - `os-open-uprn-original` — the original OS Open UPRN coordinate, untouched.
    - `auto-snapped-to-toid` — the original UPRN coordinate snapped to the centroid of its uniquely-matched TOID polygon, by Lleolydd's spatial routine. Not curator-confirmed.
    - `curator-placed` — a coordinate the curator chose from scratch (drag-to-place).
    - `curator-confirmed-original` — the curator inspected and explicitly confirmed the original OS Open UPRN coordinate as correct (no change in value, change in status).

    **7.3 New qualifier vocabulary.** Usable on `geometry`, `verified_building_toid`, and `location_verification_status` claims:

    - `verification_method` — enum: `on-site`, `aerial`, `local-knowledge`, `documentary`, `desk-derived`. Records *how* the curator reached the decision.
    - `verified_at` — date (existing date semantics). When the verification happened.
    - `cache_snapshot_id` — string referring to the OGL cache snapshot manifest the verification was made against (e.g. `lleolydd-cache-2026-05`). Makes verifications reproducible after OS data updates.

    Usable on any claim accepted via the co-sign acceptance path (`design/lleolydd.md` §12.A):

    - `field_session_id` — string referring to a Lleolydd field-session object. Names the synchronous coordination context the placement happened in.
    - `co_signed_by` — entity_id of the second curator who co-signed the placement, distinct from the original submitter. Multiple `co_signed_by` qualifiers permitted if more than two curators present.

    **7.4 New `temporal_status` qualifier.** Surfaced by the §12.D forward-scope analysis. Usable on `geometry`, `verified_building_toid`, and any future v0.3 entity-locating claim:

    - `temporal_status` — enum: `existing`, `proposed`, `historic`, `removed`. Default `existing`. Lets a single drag-to-place primitive serve both curator-correcting-existing-UPRN (`existing`) and energy-modeller-placing-future-PV (`proposed`), without changing the underlying claim shape.

    **7.5 No new entity types in this item.** The §12.D entity-type generalisation (monument / feature / open_space / infrastructure_asset) is **not** included here. It is held as a separate v0.3 schema item, gated on the explicit-boundaries Awen-positioning conversation. Lleolydd's engine is built type-agnostic so v0.3 enablement is a UI-side change only.

    **7.6 Validator updates.** The schema-layer pure functions need extending:

    - `validate_claim` — must accept the two new predicates and the four new qualifier names; reject unknown enum values; allow multiple `co_signed_by` qualifiers when present.
    - `validate_proposal` — same surface area extension. Rejects claims with `co_signed_by` in the absence of `field_session_id` (co-sign requires a named session).
    - New: `validate_field_session_id` — format and lifecycle validation for the `field_session_id` value (server-side concept, claim-level reference only).

    Validation for the new `entity_proposal` shape lives in a parallel `validate_entity_proposal` function, designed in `design/entity-proposal-shape.md` and added to the schema layer alongside this item.

    **7.7 Migration / coexistence.** Existing `geometry` claims with no `geometry_basis` qualifier remain valid; they are treated as `unspecified` for status-band derivation purposes (and surface in the unsnapped band until a curator passes over them). No data migration required.

    The `location_verification_status` predicate is materialised — derived view, kept in sync. First materialisation runs at the end of `lleolydd-cache build` once the §6.21 component is built.

    **7.8 Cross-domain effect.** Once this item lands and is materialised, downstream consumers gain a single boolean test for trust:

    - The energy study's `apply_dec_corrections.py`: filter floor-area cross-references to UPRNs whose subject building has `location_verification_status: verified`.
    - The BRA v2 subject-resolution loop: of the 303 currently-`still_ambiguous` drafts, those resolving to `verified` buildings become safe to propose; those resolving only to `auto-snapped` wait for Lleolydd confirmation.
    - HMLR Price Paid + OCOD joins: only join sale records to canonical buildings via UPRNs whose status is `verified`. `auto-snapped` joins go to a probable-but-unconfirmed bucket pending review.

    This is the kind of single-flag uplift that's hard to argue against — the alternative is each consumer carrying its own bespoke trust heuristic.

## 11. SQL DDL — full v0.1

```sql
-- DuckDB-compatible. SpatiaLite needs the GEOMETRY column adjusted.

CREATE TABLE entity (
  entity_id     VARCHAR PRIMARY KEY,
  entity_type   VARCHAR NOT NULL CHECK (entity_type IN
                ('building','street','area','town','tenancy','event',
                 'research_question','source','person')),
  uprn          BIGINT,
  toid          VARCHAR,
  visibility    VARCHAR CHECK (visibility IN ('public','restricted','private') OR visibility IS NULL),
  created_at    TIMESTAMP NOT NULL DEFAULT NOW(),
  notes         TEXT
);

CREATE TABLE predicate (
  name                    VARCHAR PRIMARY KEY,
  value_type              VARCHAR NOT NULL CHECK (value_type IN
                          ('text','int','real','date','geom','bilingual','entity_ref')),
  cardinality             VARCHAR NOT NULL CHECK (cardinality IN ('single','multi')),
  applies_to_types        VARCHAR NOT NULL,
  description_cy          VARCHAR NOT NULL,
  description_en          VARCHAR NOT NULL,
  constraint_json         VARCHAR,
  required_qualifiers     VARCHAR,
  added_at                TIMESTAMP NOT NULL DEFAULT NOW(),
  added_by                VARCHAR NOT NULL,
  deprecated_at           TIMESTAMP,
  deprecated_by           VARCHAR,
  deprecation_reason      VARCHAR,
  superseded_by_predicate VARCHAR REFERENCES predicate(name)
);

CREATE TABLE claim (
  claim_id          VARCHAR PRIMARY KEY,
  subject_id        VARCHAR NOT NULL REFERENCES entity(entity_id),
  predicate         VARCHAR NOT NULL REFERENCES predicate(name),
  value_text        VARCHAR,
  value_int         BIGINT,
  value_real        DOUBLE,
  value_date        DATE,
  value_date_text   VARCHAR,
  value_geom        GEOMETRY,
  value_cy          VARCHAR,
  value_en          VARCHAR,
  value_entity_ref  VARCHAR REFERENCES entity(entity_id),
  qualifiers_json   VARCHAR,
  source_id         VARCHAR NOT NULL REFERENCES entity(entity_id),
  recorded_by       VARCHAR NOT NULL,
  recorded_at       TIMESTAMP NOT NULL DEFAULT NOW(),
  confidence        VARCHAR NOT NULL CHECK (confidence IN ('high','medium','low')),
  evidence_uri      VARCHAR,
  superseded_by     VARCHAR REFERENCES claim(claim_id),
  status            VARCHAR NOT NULL DEFAULT 'active'
                    CHECK (status IN ('active','superseded','disputed','withdrawn'))
);

CREATE INDEX claim_subject_predicate_idx ON claim(subject_id, predicate);
CREATE INDEX claim_status_idx ON claim(status);
CREATE INDEX claim_value_entity_ref_idx ON claim(value_entity_ref);

CREATE TABLE prawf_log (
  log_id         VARCHAR PRIMARY KEY,
  ts             TIMESTAMP NOT NULL DEFAULT NOW(),
  actor          VARCHAR NOT NULL,
  action         VARCHAR NOT NULL,
  target_id      VARCHAR NOT NULL,
  payload_json   VARCHAR NOT NULL,
  prev_hash      VARCHAR,
  this_hash      VARCHAR NOT NULL
);

CREATE VIEW current_claim AS
  SELECT * FROM claim
  WHERE status IN ('active','disputed')
  AND superseded_by IS NULL;

-- Coverage metric for public read API
CREATE VIEW cy_coverage AS
  SELECT
    p.name AS predicate,
    COUNT(*) FILTER (WHERE c.value_cy IS NOT NULL) AS cy_populated,
    COUNT(*) AS total,
    100.0 * COUNT(*) FILTER (WHERE c.value_cy IS NOT NULL) / NULLIF(COUNT(*),0) AS pct
  FROM claim c
  JOIN predicate p ON p.name = c.predicate
  WHERE p.value_type = 'bilingual'
  AND c.status = 'active'
  GROUP BY p.name;
```

---

**Next steps after this draft is reviewed:**

1. Mark up §3.5 starter predicate set — anything missing, anything to remove, anything to rename.
2. Decide whether to bootstrap the GitHub repo now with v0.1 as the initial commit.
3. Re-do the Tŷ Newyddion record cleanly under v0.1 as the first canonical Craidd record (a tighter version of §7 above, with all sources properly created as entities first).
4. Order the Pi.
